<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="calendar">
  	<resultMap type="calendarVo" id="calendarMap">
  		<result property="memberId" column="MEMBER_ID" />
  		<result property="lessonNo" column="LESSON_NO" />
  		<result property="lessonName" column="LESSON_NAME" />
  		<result property="lessonTime" column="LESSON_TIME" />
  		<result property="lessonStart" column="LESSON_START" />
  	</resultMap>
  	
  	<select id="selectCalendar" resultMap="calendarMap">
  		SELECT M.MEMBER_ID, P.LESSON_NO, L.LESSON_NAME, L.LESSON_TIME, L.LESSON_START
  		FROM MEMBER M, LESSON L, PAYMENT P
  		WHERE M.MEMBER_ID = P.MEMBER_ID AND L.LESSON_NO = P.LESSON_NO AND M.MEMBER_ID = #{memberId}
  	</select>
  	
</mapper>